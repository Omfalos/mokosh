# Full-house notes

Design notes for the full-house fixture. Mentions `math.js` and `types.ts` in prose so the
Markdown code-span resolver has something to resolve.

See `math.js` for the shared helpers and `types.ts` for the shared type aliases.
